@page
@model FlowOps.Web.Pages.Organization.SwitchModel
@{
    // POST-only operation (Step 5) — this page has no OnGet and renders nothing; it exists only
    // so the switcher form in _Layout.cshtml has a route to post to.
}
